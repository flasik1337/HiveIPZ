import 'package:flutter/material.dart';

class HiveTextStyles {
  /// Używana do tytułów
  ///
  /// Kolor: biały, rozmiar: 28, bold
  static const title = TextStyle(
    color: Colors.white,
    fontSize: 28,
    fontWeight: FontWeight.bold,
  );

  /// Używana jako podstawowy styl dal pól tekstowych
  ///
  /// Kolor: biały, rozmiar: 20, regular
  static const regular = TextStyle(
    color: Colors.white,
    fontSize: 20,
  );
}